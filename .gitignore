.DS_Store

# SwiftPM and Xcode build outputs
.build/
.swiftpm/
DerivedData/
*.xcodeproj
xcuserdata/

# Resolved dependency graph: this is a library, and the dependencies are
# currently local path packages, so the lock file is not meaningful to share.
Package.resolved

# Editor and local environment state
.vscode/
.idea/
.claude/
*.swp
*.xcuserstate

# Local-only planning, fixtures, scratch data, and agent instructions
local/

# Generated reports and coverage
coverage/
*.profdata
*.profraw
*.xcresult
